#include <bits/stdc++.h>
using namespace std;

void deleteMiddleElementFormStack(int element, int *arr, int size)
{
    stack<int> s;
    if (s.empty())
    {
        cout << "Stack is empty" << endl;
    }
}